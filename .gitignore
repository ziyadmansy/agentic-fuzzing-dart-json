# Dart / Flutter
.dart_tool/
.packages
build/
*.lock.bak

# Python (for the ported refinement-loop pipeline)
.venv/
__pycache__/
*.pyc

# Experiment output: the seeded campaigns underlying the paper's tables
# (artifacts/repeated/) ARE committed -- the paper explicitly claims their
# release. Only exclude loose, superseded exploratory runs from before the
# seeded scripts existed.
/artifacts/*.jsonl
/artifacts/*.manifest.json
/artifacts/*.reproducibility.json
/artifacts/*.reproducibility.md

# LaTeX build output (regenerate with: tectonic paper/main.tex) -- contains
# the real author block, unlike the tracked anonymized submission copies.
paper/main.pdf

# OS
.DS_Store

# 
.env
